window['document'] //window는 브라우저, document는 페이지(탭)

var newwindow = window.open() // 새 창

window.alert('addasa') //window는 전역 객체이므로 생략 가능 

var a='b'
window.a; //'b' 전역 변수는 전역 객체의 속성이 됩니다. (함수 바깥)

document
console.dir(document)
'DOM 객체'
'document object model'