js에서 비동기는 코드 상의 순서대로 실행되지 않는 코드를 의미.

push: 마지막에 추가, pop: 마지막 것 뽑기
unshift: 처음에 추가, shift: 처음 것 뽑기

splice(위치, 개수) 하면 위치로부터 개수만큼 배열에서 뽑아요

문자.split(구분자) -> 배열
배열.join(구분자) -> 문자

배열.indexOf(값) 값의 위치를 알 수 있어요 없으면 -1

코딩이 끝나고 코드를 정리하는 것을 리팩토링이라고 합니다.