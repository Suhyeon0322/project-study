데이터와 화면을 따로 생각하되 둘을 일치시키는 작업이 필요해요.
마우스 오른쪽 클릭 이벤트는 contextmenu 이벤트입니다.

이벤트리스너를 직접 다는 대상이 커런트타겟, 이벤트가 실제로 발생하는 애가 타겟.

concat은 배열과 배열을 합쳐서 "새로운" 배열을 만들어요.

scope 변수에 접근 가능한 범위
변수는 자신을 감싸고 있는 함수 바깥으로 빠져나갈 수 없다!

var은 선언한 함수 내부에서만 유효해요. 펑션(함수) 스코프라고 불러요.

스코프 간의 상하관계를 스코프 체인이라고 불러요.

코드가 적힌 순간 스코프가 정해져요.
이것을 렉시컬 스코프 or 정적 스코핑라고 불러요.

var 말고 function 선언도 스코프 적용을 받습니다.

함수와 함수가 접근할 수 있는 스코프가 클로저 관계를 맺어요

함수 안의 변수는 "실행"될 때 값이 결정됩니다.

매개변수도 변수로 치시면 됩니다. (실인자 값으로 초기화)

```function (var i=0; i<100; i++) {
    (function 클로저(j) {
        setTimeout(function() {
            console.log(j);
        }, j*1000);
    })(i);
}```

태그.classList로 태그의 클래스에 접근, add나 remove로 추가 삭제

플래그는 코드의 흐름을 좌우하는 변수를 가리킵니다.